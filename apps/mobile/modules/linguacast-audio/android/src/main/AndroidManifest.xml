<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
  <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
  <!--
    The recovery clock. A foreground service keeps the process, not the CPU: with no audio
    flowing there is nothing else holding the device awake, and a suspended CPU is a
    JavaScript runtime that cannot notice a network change until the guest looks at it.
  -->
  <uses-permission android:name="android.permission.WAKE_LOCK" />

  <application>
    <!--
      mediaPlayback and not microphone: this is a receive-only listener, and the wrong type
      invites a store review question the product cannot answer. Declared here rather than in
      app.json so the app's configuration does not have to carry the module's own needs.
    -->
    <service
      android:name=".ListeningService"
      android:exported="false"
      android:foregroundServiceType="mediaPlayback">
      <intent-filter>
        <action android:name="android.intent.action.MEDIA_BUTTON" />
      </intent-filter>
    </service>

    <!--
      Routes the notification's own play and pause presses back into the media session.
      `buildMediaButtonPendingIntent` addresses this receiver, so without it the buttons
      are drawn and do nothing.
    -->
    <receiver
      android:name="androidx.media.session.MediaButtonReceiver"
      android:exported="true">
      <intent-filter>
        <action android:name="android.intent.action.MEDIA_BUTTON" />
      </intent-filter>
    </receiver>
  </application>
</manifest>
